# Ralph Progress Log
Started: 2026-03-29

## Codebase Patterns
- lynx.getTextInfo(text, { fontSize: "16px", fontFamily?: "Inter" }) returns { width: number, content?: string[] } — synchronous on main thread
- Module-level font context (currentFontSizeStr, currentFontFamily) must be set before calling getSegmentMetrics
- Root tsconfig.json targets ES2020 with lib ES2020 + ES2022.Intl — supports Set, Map, codePointAt, Array.from, Unicode regex flag, Intl.Segmenter types
- Rspeedy project uses `lynx.config.ts` (not `rspeedy.config.ts`) for build configuration
- Entry point is `src/index.tsx`, demo pages live under `pages/`
- `src/tsconfig.json` includes `../pages/**/*.tsx` so pages are typechecked with src
- `@formatjs/intl-segmenter` v12.x is the current major (v11 is no longer available)
- `tsc --build` is the typecheck command (project references mode)
- `import '@formatjs/intl-segmenter/polyfill-force.js'` patches Intl.Segmenter globally; use `ES2022.Intl` in lib for TS types
- Rspeedy build output goes to `dist/main.lynx.bundle`
- lynx.getTextInfo supports maxWidth as string ("300px") and returns content?: string[] with line-broken text
- TextInfo type: { fontSize: string, fontFamily?: string, maxWidth?: string, maxLine?: number }
- PrimJS does not have the `Intl` global — must create `globalThis.Intl = {}` before `@formatjs/intl-segmenter/polyfill-force.js`
- Multi-entry Rspeedy config: `source.entry: { 'page-name': './pages/page-name.tsx' }` → produces `page-name.lynx.bundle`
- Each entry page file must call `root.render(<Component />)` + `import.meta.webpackHot.accept()`
- Lynx `<text>` does NOT support `position: 'absolute'` — wrap in `<view>` for positioning
- For manually rendering lines: use stacked `<view>` with `height: LINE_HEIGHT` instead of absolute positioning

---

## 2026-03-29 - US-001
- Scaffolded Rspeedy project with TypeScript using `create-rspeedy` react-ts template as reference
- Created `src/` for library code and `pages/` for demo pages
- Hello-world page at `pages/hello-world.tsx` renders `<text>` elements
- Entry point at `src/index.tsx` renders the hello-world page
- Installed `@formatjs/intl-segmenter` ^12.2.1 as dependency
- Files changed: package.json, lynx.config.ts, tsconfig.json, tsconfig.node.json, src/tsconfig.json, src/rspeedy-env.d.ts, src/index.tsx, pages/hello-world.tsx, .gitignore
- **Learnings for future iterations:**
  - `create-rspeedy` supports non-interactive mode: `pnpm create rspeedy@latest <dir> --template react-ts`
  - The scaffold template puts everything in `src/` — we split library code (src/) from pages (pages/) for this project
  - `@lynx-js/react` provides React primitives (useState, useEffect, etc.) — import from there, not `react`
  - Build and typecheck both pass with zero config beyond the template defaults
---

## 2026-03-29 - US-002
- Ported core types and whitespace normalization from pretext/src/analysis.ts (lines 1-385)
- Exported types: WhiteSpaceMode, SegmentBreakKind, MergedSegmentation, AnalysisChunk, TextAnalysis, AnalysisProfile
- Exported functions: normalizeWhitespaceNormal, normalizeWhitespacePreWrap, classifySegmentBreakChar, splitSegmentByBreakKind, getWhiteSpaceProfile
- Files changed: src/analysis.ts (new)
- **Learnings for future iterations:**
  - The original analysis.ts has internal types (SegmentationPiece, WhiteSpaceProfile) that need to be included but aren't part of the public API
  - getWhiteSpaceProfile was made export since classifySegmentBreakChar and splitSegmentByBreakKind both take WhiteSpaceProfile as parameter — callers need a way to create one
  - US-003 and US-004 will add more to the same src/analysis.ts file (character sets, merge pipeline, analyzeText)
---

## 2026-03-29 - US-003
- Ported character sets and merge helper predicates from pretext/src/analysis.ts (lines 97-319)
- Exported: isCJK, kinsokuStart, kinsokuEnd, leftStickyPunctuation, isLeftStickyPunctuationSegment, isForwardStickyClusterSegment, isEscapedQuoteClusterSegment, splitTrailingForwardStickyCluster, endsWithClosingQuote
- Internal helpers: containsArabicScript, isCJKLineStartProhibitedSegment, isRepeatedSingleCharRun, endsWithArabicNoSpacePunctuation, endsWithMyanmarMedialGlue, splitLeadingSpaceAndMarks, arabicScriptRe, combiningMarkRe, decimalDigitRe, forwardStickyGlue, arabicNoSpaceTrailingPunctuation, myanmarMedialGlue, closingQuoteChars
- Files changed: src/analysis.ts (appended ~220 lines), tsconfig.json (added target/lib ES2020)
- **Learnings for future iterations:**
  - Default TS target is ES3 which lacks Set, codePointAt, Array.from, and Unicode regex flag `u` — needed to add `"target": "ES2020"` and `"lib": ["ES2020"]` to root tsconfig.json
  - Used Unicode escape codes (e.g. `\u201C`) instead of literal smart quotes to avoid encoding issues — functionally identical
  - The `decimalDigitRe` and other internal helpers are only used by the merge pipeline (US-004) but were included here since they're part of the same character-set block
---

## 2026-03-29 - US-004
- Ported full merge pipeline and analyzeText from pretext/src/analysis.ts (lines 320-1008)
- Added `import '@formatjs/intl-segmenter/polyfill-force.js'` to patch Intl.Segmenter globally
- Added word segmenter management: sharedWordSegmenter, getSharedWordSegmenter, clearAnalysisCaches, setAnalysisLocale
- Ported all merge functions: mergeUrlLikeRuns, mergeUrlQueryRuns, mergeNumericRuns, mergeAsciiPunctuationChains, splitHyphenatedNumericRuns, mergeGlueConnectedTextRuns, carryTrailingForwardStickyAcrossCJKBoundary
- Ported buildMergedSegmentation, compileAnalysisChunks, analyzeText
- Files changed: src/analysis.ts (appended ~648 lines), tsconfig.json (added ES2022.Intl to lib)
- **Learnings for future iterations:**
  - `@formatjs/intl-segmenter/polyfill-force.js` patches `Intl.Segmenter` globally at runtime, but TS needs `ES2022.Intl` in the lib array for type definitions
  - The polyfill's `polyfill-force.d.ts` only exports `{}` — it doesn't augment the Intl namespace
  - The original uses `'—'` literal em-dash in buildMergedSegmentation; used `'\u2014'` escape for consistency
  - The `×` multiplication sign in numericJoinerChars was escaped as `'\u00D7'`
  - All merge functions are 1:1 ports with zero adaptation needed — they operate on pure MergedSegmentation data
---

## 2026-03-29 - US-005
- Ported measurement.ts from pretext, replacing Canvas measureText() with lynx.getTextInfo()
- Added parseFontFamily() to extract font family from CSS font shorthand strings
- getEngineProfile() returns single Lynx profile (no UA sniffing): lineFitEpsilon=0.005, all booleans false
- Emoji correction set to 0 for MVP — skipped getEmojiCorrection, countEmojiGraphemes, isEmojiGrapheme
- Module-level currentFontSizeStr/currentFontFamily replaces Canvas ctx.font context
- getFontMeasurementState sets module-level font context before getSegmentMetrics calls getTextInfo
- Files changed: src/measurement.ts (new)
- **Learnings for future iterations:**
  - lynx.getTextInfo(text, { fontSize: string, fontFamily?: string }) returns { width: number, content?: string[] }
  - fontSize must be a string with unit like "16px", not a bare number
  - fontFamily is optional — omit it to use system default
  - The `lynx` global is typed via @lynx-js/types transitively through @lynx-js/rspeedy/client reference
  - No need for getMeasureContext() / OffscreenCanvas / Canvas — getTextInfo is synchronous on main thread
  - getCorrectedSegmentWidth simply returns metrics.width when emojiCorrection=0 (same as original fast path)
---

## 2026-03-29 - US-006
- Ported line-break.ts 1:1 from pretext — zero platform dependencies, only import paths changed
- Exported: countPreparedLines, walkPreparedLines, layoutNextLineRange, normalizeLineStart
- Types exported: LineBreakCursor, PreparedLineBreakData, InternalLayoutLine
- Internal helpers: countPreparedLinesSimple, walkPreparedLinesSimple, layoutNextLineRangeSimple (fast paths for simple text)
- Full path handles: soft-hyphen, tab, hard-break, preserved-space, trailing whitespace hang, grapheme-level word overflow
- Files changed: src/line-break.ts (new, 1056 lines)
- **Learnings for future iterations:**
  - line-break.ts has zero platform dependencies — truly pure arithmetic over PreparedLineBreakData
  - Only imports: SegmentBreakKind type from analysis, getEngineProfile from measurement
  - The simple fast path (countPreparedLinesSimple, walkPreparedLinesSimple, layoutNextLineRangeSimple) handles text+space+ZWSP only
  - The full path adds soft-hyphen, tab, hard-break, preserved-space support via chunk-based iteration
---

## 2026-03-29 - US-007
- Ported layout.ts from pretext: core prepare/layout API and all rich-path functions
- Stubbed computeSegmentLevels (bidi) to return null for MVP
- Exported public API: prepare, prepareWithSegments, layout, layoutWithLines, walkLineRanges, layoutNextLine, clearCache, setLocale
- Exported types: PreparedText, PreparedTextWithSegments, LayoutCursor, LayoutResult, LayoutLine, LayoutLineRange, LayoutLinesResult, PrepareOptions, PreparedLineChunk
- Internal helpers: measureAnalysis, mapAnalysisChunksToPreparedChunks, prepareInternal, createEmptyPrepared, buildLineTextFromRange, materializeLayoutLine, toLayoutLineRange
- Files changed: src/layout.ts (new, 621 lines)
- **Learnings for future iterations:**
  - layout.ts has zero direct platform calls — all measurement goes through measurement.ts
  - computeSegmentLevels (bidi) stubbed to null — safe because line-break engine never reads segLevels
  - Included both US-007 (prepare/layout) and US-008 (rich-path: layoutWithLines, walkLineRanges, layoutNextLine, prepareWithSegments) in same file since they're tightly coupled in the original
  - The WeakMap<PreparedTextWithSegments, Map<number, string[]>> pattern caches grapheme splits per prepared handle without leaking memory
---

## 2026-03-29 - US-008
- All rich layout functions were already implemented in US-007 (same file, tightly coupled)
- Verified: prepareWithSegments, layoutWithLines, walkLineRanges, layoutNextLine all present and exported
- Verified: LayoutLine has { text, width, start: LayoutCursor, end: LayoutCursor }
- Verified: buildLineTextFromRange appends '-' for soft-hyphen lines
- Verified: All required types exported (PreparedText, PreparedTextWithSegments, LayoutCursor, LayoutResult, LayoutLine, LayoutLineRange, LayoutLinesResult, PrepareOptions)
- Typecheck passes with zero errors
- Files changed: none (already complete from US-007)
- **Learnings for future iterations:**
  - When stories are tightly coupled in the source, implementing them together avoids artificial splitting
  - PrepareProfile/profilePrepare from reference are diagnostic-only exports, not needed for the core API
---

## 2026-03-29 - US-009
- Created accuracy validation page at pages/accuracy.tsx
- Created shared test corpus at src/test-data.ts with 21 diverse texts (English, CJK, Japanese, Korean, emoji, mixed scripts, edge cases)
- Tests 10 widths (50px-500px in 50px steps) per text at 16px font size
- Uses lynx.getTextInfo(text, { fontSize, maxWidth }) as native oracle
- Compares line-by-line: native.content[i].trim() vs layoutWithLines().lines[i].text.trim()
- Reports: overall pass rate, English-specific pass rate, per-test pass/fail with first divergent line highlighted
- Logs mismatches to console with text, width, expected/actual line content
- Toggle to show all results or failures only
- Updated src/index.tsx to render accuracy page
- Build and typecheck pass
- Files changed: src/test-data.ts (new), pages/accuracy.tsx (new), src/index.tsx (updated)
- **Learnings for future iterations:**
  - lynx.getTextInfo accepts maxWidth as string ("300px"), returns content?: string[] with native line-broken text
  - TextInfo interface: { fontSize: string, fontFamily?: string, maxWidth?: string, maxLine?: number }
  - Empty texts are skipped (getTextInfo with empty string may return no content)
  - The accuracy page is the primary verification gate before building demos
---

## 2026-03-29 - US-010
- Created basic height measurement demo page at pages/basic-height.tsx
- Uses prepare() + layout() to compute pretext height and lineCount
- Uses lynx.getTextInfo() with maxWidth as native oracle for comparison
- +/− buttons to change maxWidth in 20px steps (80px–500px range)
- Shows native <text> element rendered at constrained width
- Side-by-side comparison cards: pretext vs native height/lineCount
- MATCH/MISMATCH status indicator with height diff details
- Native line breakdown shows per-line text content
- Updated src/index.tsx to render BasicHeightPage
- Files changed: pages/basic-height.tsx (new), src/index.tsx (updated)
- **Learnings for future iterations:**
  - prepare() is called on every render for simplicity; for perf, memoize with useMemo keyed on text+font
  - lynx.getTextInfo with maxWidth returns content[] which gives native line count directly
  - Native height = nativeLineCount * lineHeight matches the pretext convention
  - Lynx <view> supports gap property for flexbox spacing
---

## 2026-03-29 - US-011
- Created layoutWithLines manual rendering demo at pages/layout-with-lines.tsx
- Uses prepareWithSegments() (memoized via useMemo) + layoutWithLines() on every width change
- Each line rendered as absolutely positioned <text> element within a sized container
- Line details table shows line index, text content, width, and segment range
- Native <text> comparison block at bottom for visual verification
- +/− buttons change maxWidth (80–500px in 20px steps)
- Updated src/index.tsx to render LayoutWithLinesPage
- Files changed: pages/layout-with-lines.tsx (new), src/index.tsx (updated), tasks/prd.json (updated)
- **Learnings for future iterations:**
  - useMemo with empty deps array memoizes prepare() across re-renders — layout is pure arithmetic on cached data
  - position: 'absolute' with top: i * lineHeight positions lines correctly in Lynx
  - LayoutLine.start/end LayoutCursor gives segment-level debugging info (segmentIndex, graphemeIndex)
---

## 2026-03-29 - US-012
- Created shrinkwrap demo page at pages/shrinkwrap.tsx
- Uses walkLineRanges to find widest line width (CSS fit-content equivalent)
- Binary-search finds tightest container width preserving same line count (shrinkwrap)
- Side-by-side visual comparison: maxWidth container vs shrinkwrap container
- Wasted pixel area shown numerically with color-coded status
- +/− buttons change maxWidth (80–500px in 20px steps)
- Explanatory text describes the capability gap vs native Lynx layout
- Updated src/index.tsx to render ShrinkwrapPage
- Files changed: pages/shrinkwrap.tsx (new), src/index.tsx (updated)
- **Learnings for future iterations:**
  - walkLineRanges is ideal for computing max line width without materializing line text strings
  - Binary search for shrinkwrap: search for minimum width where lineCount stays the same as at maxWidth
  - The bubbles-shared.ts in pretext has the same pattern (collectWrapMetrics + findTightWrapMetrics)
  - Shrinkwrap is purely arithmetic — no native calls in the resize path
---

## 2026-03-29 - US-013
- Created variable-width flow demo at pages/variable-flow.tsx
- Uses layoutNextLine in a loop with varying maxWidth per line to flow text around a rectangular obstacle
- Lines overlapping the obstacle (top-right, 120x100px) get narrower width; lines below get full maxWidth
- Each line rendered as absolutely positioned <text> element
- Cursor continuity: line.end becomes next line's start cursor
- +/- buttons change container maxWidth (200-500px); flow recalculates on every change
- Summary shows total/narrow/full-width line counts; line detail table shows per-line width info
- Updated src/index.tsx to render VariableFlowPage
- Files changed: pages/variable-flow.tsx (new), src/index.tsx (updated)
- **Learnings for future iterations:**
  - layoutNextLine(prepared, cursor, maxWidth) returns LayoutLine | null — the streaming API
  - Variable-width flow: compute available width per line based on Y position, pass different maxWidth each iteration
  - The obstacle overlap check: `y < obstacleHeight + gap` determines if a line is beside the obstacle
  - This pattern (varying maxWidth per line) is the key differentiator vs native <text> layout
---

## 2026-03-29 - US-014
- Ported bubbles-shared.ts from pretext/pages/demos/bubbles-shared.ts
- Pure logic port — no DOM APIs, only import path changed ('../../src/layout.ts' → '../../src/layout')
- Exported: prepareBubbleTexts, collectWrapMetrics, findTightWrapMetrics, computeBubbleRender, getMaxChatWidth, formatPixelCount
- Exported types: WrapMetrics, PreparedBubble, BubbleRenderWidths, BubbleRenderState
- Exported constants: FONT, LINE_HEIGHT, PADDING_H, PADDING_V, BUBBLE_MAX_RATIO, PAGE_MAX_WIDTH, DESKTOP_PAGE_MARGIN, MOBILE_PAGE_MARGIN, GRID_GAP, PANEL_PADDING_X
- Created pages/demos/ directory (first file in this subdirectory)
- Files changed: pages/demos/bubbles-shared.ts (new)
- **Learnings for future iterations:**
  - bubbles-shared.ts is 100% platform-agnostic — uses only layout/walkLineRanges/prepareWithSegments from src/layout
  - findTightWrapMetrics uses binary search on layout() lineCount — same pattern as shrinkwrap.tsx demo
  - pages/demos/*.ts files are included in typecheck via src/tsconfig.json include pattern "../pages/**/*.ts"
---

## 2026-03-29 - US-015
- Created bubbles shrinkwrap chat page at pages/demos/bubbles.tsx
- Replaced all DOM APIs (document.getElementById, HTMLDivElement, requestAnimationFrame, CSS variables) with ReactLynx state-driven components
- Hardcoded 7 chat bubble texts matching the original HTML (English, Korean, Arabic, mixed scripts)
- Two panels: CSS fit-content (uses cssWidth from computeBubbleRender) vs Pretext shrinkwrap (uses tightWidth)
- +/− buttons change chatWidth (220–500px in 20px steps), all bubbles reflow live via computeBubbleRender
- Wasted pixels comparison: CSS panel shows total wasted px², shrinkwrap panel shows 0
- Explanation section on why CSS can't do this
- Updated src/index.tsx to render BubblesPage
- Files changed: pages/demos/bubbles.tsx (new), src/index.tsx (updated), tasks/prd.json (updated)
- **Learnings for future iterations:**
  - computeBubbleRender from bubbles-shared.ts does all the heavy lifting — returns per-bubble cssWidth and tightWidth
  - In Lynx, set explicit `width` on bubble views instead of CSS `fit-content` — Lynx doesn't support fit-content
  - The CSS panel uses cssWidth (max line width + padding) to simulate fit-content behavior
  - BUBBLE_DIRECTIONS array maps sent/received alignment — no DOM class queries needed
---

## 2026-03-29 - US-016
- Ported wrap-geometry.ts from pretext/pages/demos/wrap-geometry.ts
- All pure geometry functions ported 1:1: getPolygonIntervalForBand, getRectIntervalsForBand, carveTextLineSlots, transformWrapPoints, isPointInPolygon, getPolygonXsAtY
- Replaced getWrapHull (uses OffscreenCanvas/Image) with precomputed hull data approach
- Created hull-data.ts with precomputed hull points for OpenAI and Claude logos (4 hull variants)
- Added registerLogoHulls() to register hull data keyed by asset URL for runtime lookup
- Created scripts/generate-hulls.mjs to regenerate hull data from SVGs using sharp
- Files changed: pages/demos/wrap-geometry.ts (new), pages/demos/hull-data.ts (new), scripts/generate-hulls.mjs (new)
- **Learnings for future iterations:**
  - OffscreenCanvas and Image APIs are not available in Lynx — precompute any rasterization-dependent data
  - Hull data is normalized to 0-1 coordinates, so it's resolution-independent
  - The dynamic-layout demo (US-017) needs 4 hull variants: openaiLayout (sr=6), claudeLayout (sr=6), openaiHit (sr=3), claudeHit (sr=5)
  - registerLogoHulls() must be called with the actual asset URLs before getWrapHull() is used
  - sharp npm package works well for Node.js SVG rasterization on macOS (prebuilt binaries)
---

## 2026-03-29 - US-017
- Created dynamic editorial layout page at pages/demos/dynamic-layout.tsx
- Created BODY_COPY text at pages/demos/dynamic-layout-text.ts (direct port from reference)
- Ported all pure layout functions 1:1 from reference: buildLayout, evaluateLayout, layoutColumn, fitHeadlineFontSize, getObstacleIntervals, getLogoProjection
- Headline font size binary-search fitted to avoid mid-word breaks
- Body text flows left column → right column via layoutNextLine cursor continuity
- Both columns route around logo obstacles using polygon interval carving from wrap-geometry
- Tap on logo triggers rotation animation with text reflow (setTimeout-based animation loop)
- Width/height controls (+/−) allow viewport resize; switches between single-column (<760px) and two-column (≥760px) modes
- Logo placeholders rendered as colored circular views (SVG images not available in Lynx; hulls are precomputed)
- Replaced all DOM APIs (document.createElement, HTMLElement, requestAnimationFrame, window events) with ReactLynx state-driven components
- Updated src/index.tsx to render DynamicLayoutPage
- Files changed: pages/demos/dynamic-layout-text.ts (new), pages/demos/dynamic-layout.tsx (new), src/index.tsx (updated)
- **Learnings for future iterations:**
  - For animation in Lynx, use setTimeout loop + refs for mutable state + a renderTick counter via useState to trigger re-renders
  - animateFnRef pattern: assign the latest closure to a ref every render, setTimeout calls animateFnRef.current() to always get the latest version
  - Lynx transform property accepts CSS string format: `rotate(Xdeg)` — works on view elements
  - Hull data can be imported directly from hull-data.ts instead of going through getWrapHull/registerLogoHulls when the keys are known at compile time
  - For complex demos with many absolutely positioned text elements, the React reconciler handles key-based updates efficiently
  - useMemo + useRef<Map> pattern works well for caching prepared texts across renders without re-measuring
---

## 2026-03-29 - QUALITY GATE UPDATE
- **NEW REQUIREMENT**: ALL commits must pass BOTH `tsc --build` AND `pnpm build`
- PrimJS does not support Unicode property escapes (\p{...}) — use explicit Unicode ranges
- DevTool verification is MANDATORY for UI stories — if build fails, the story is NOT complete
- Added US-018 (fix regex) and US-019 (DevTool verification) to prd.json
---

## 2026-03-29 - US-018
- Verified all Unicode property escape regexes (\p{...}) were already replaced with explicit Unicode ranges in prior stories
- src/analysis.ts: arabicScriptRe, combiningMarkRe, decimalDigitRe all use explicit Unicode ranges (no \p{})
- src/measurement.ts: maybeEmojiRe uses explicit emoji Unicode ranges (no \p{})
- splitLeadingSpaceAndMarks uses per-character combiningMarkRe.test(ch) — equivalent to original /^\p{M}+$/u
- tsc --build passes, pnpm build passes (538.4 kB bundle), no PrimJS bytecode errors
- Files changed: tasks/prd.json (mark passes: true), tasks/progress.txt (this entry)
- **Learnings for future iterations:**
  - Unicode property escapes were proactively replaced during US-003 (character sets) and US-005 (measurement) — good practice to do at port time
  - The combiningMarkRe expanded form is very long (~2KB) but compresses well in the bundle
  - Always verify pnpm build after typecheck — PrimJS compilation catches things tsc doesn't
---

## 2026-03-29 - US-019 (BLOCKED)
- **Status:** Cannot complete — no Lynx device connected for DevTool verification
- Verified: pnpm build passes (538.4 kB bundle)
- Verified: rspeedy dev starts successfully (http://172.20.5.50:3004/main.lynx.bundle)
- Verified: All 8 demo page files exist (hello-world, accuracy, basic-height, layout-with-lines, shrinkwrap, variable-flow, bubbles, dynamic-layout)
- Verified: tsc --build passes with zero errors
- **BLOCKED:** `list-clients` returns [] — no Lynx client connected, cannot take screenshots
- **To complete:** Connect a Lynx-enabled device/simulator, then use devtool take-screenshot for each page
- Files changed: tasks/progress.txt (this entry)
---

## 2026-03-29 - QUALITY GATE ENFORCEMENT (Round 2)
- **ALL stories** now require: `tsc --build` + `pnpm build` in acceptance criteria
- **Demo stories (US-010 to US-017)** now also require: DevTool screenshot verification
- Reset US-010 through US-017 to passes=false — they need build + screenshot re-verification
- Removed US-019 (redundant — screenshot gate folded into each demo story)
- Library stories (US-001 to US-009, US-018) remain passes=true — build confirmed passing at US-018
- **To verify a demo story:**
  1. Run `pnpm build` — must succeed
  2. Run `rspeedy dev` to start dev server
  3. Use DevTool `take-screenshot` on the demo page
  4. Confirm the screenshot shows expected rendering
---

## 2026-03-29 - DEVICE AVAILABLE
- Lynx Explorer connected: iOS 26.2, iPhone, port 8901
- DevTool screenshot verification is now unblocked
- Command: `node ~/.agents/skills/devtool/scripts/index.mjs take-screenshot --client localhost:8901 --session <sid> --output <path>`
- First: `node ~/.agents/skills/devtool/scripts/index.mjs list-sessions --client localhost:8901` to find sessions
- To open a page: `node ~/.agents/skills/devtool/scripts/index.mjs open-url --client localhost:8901 --url <bundle_url>`
---

## 2026-03-29 - DEVTOOL USAGE (CRITICAL - READ THIS)
- **DO NOT use `npx devtool` — it does not exist in this project**
- **USE THIS EXACT COMMAND** to list clients:
  ```
  node /Users/bytedance/.agents/skills/devtool/scripts/index.mjs list-clients
  ```
- To list sessions for a client:
  ```
  node /Users/bytedance/.agents/skills/devtool/scripts/index.mjs list-sessions --client localhost:8901
  ```
- To open a URL in the connected Lynx Explorer:
  ```
  node /Users/bytedance/.agents/skills/devtool/scripts/index.mjs open-url --client localhost:8901 --url "http://localhost:3004/main.lynx.bundle?launchUrl=pages/demos/hello-world"
  ```
- To take a screenshot:
  ```
  node /Users/bytedance/.agents/skills/devtool/scripts/index.mjs take-screenshot --client localhost:8901 --session <session_id> --output /tmp/screenshot-<page>.png
  ```
- **Workflow for each demo story verification:**
  1. `pnpm build` — must pass
  2. Start dev server: `rspeedy dev` (background it)
  3. `node /Users/bytedance/.agents/skills/devtool/scripts/index.mjs list-clients` — verify device connected
  4. `node /Users/bytedance/.agents/skills/devtool/scripts/index.mjs open-url --client localhost:8901 --url "http://localhost:3004/main.lynx.bundle?launchUrl=pages/demos/<page-name>"` — load the page
  5. `node /Users/bytedance/.agents/skills/devtool/scripts/index.mjs list-sessions --client localhost:8901` — get session id
  6. `node /Users/bytedance/.agents/skills/devtool/scripts/index.mjs take-screenshot --client localhost:8901 --session <sid> --output /tmp/screenshot-<page>.png` — capture
  7. Verify screenshot shows expected content
- **Device is currently connected:** iOS 26.2 iPhone Simulator at localhost:8901
---

## 2026-03-29 - MULTI-ENTRY PROBLEM (MUST FIX FIRST)
- **Problem:** Only `src/index.tsx` is compiled into `main.lynx.bundle`. Demo pages under `pages/` are NOT accessible.
- **Impact:** DevTool screenshot verification is impossible because demo pages have no URL to open.
- **Required fix:** Configure Rspeedy for multi-page output so each demo page gets its own bundle.
- **Rspeedy multi-page docs:** Use `environments` config in rspeedy.config.ts. Each entry produces a separate `.lynx.bundle`.
- **Example config pattern:**
  ```ts
  import { defineConfig } from '@lynx-js/rspeedy'
  export default defineConfig({
    environments: {
      'basic-height': { source: { entry: { index: './pages/basic-height.tsx' } } },
      'layout-with-lines': { source: { entry: { index: './pages/layout-with-lines.tsx' } } },
      // ... etc for each demo page
    },
    plugins: [...]
  })
  ```
- After build, each page would be at: `http://localhost:<port>/<page-name>.lynx.bundle`
- **Alternatively:** Make src/index.tsx a simple page router that dynamically imports based on a URL parameter.
- **This must be fixed before any demo story can be verified with screenshots.**
---

## 2026-03-29 - US-010 (VERIFIED)
- Fixed multi-entry problem: configured `source.entry` in lynx.config.ts for all 8 pages (main, accuracy, basic-height, layout-with-lines, shrinkwrap, variable-flow, bubbles, dynamic-layout)
- Added `root.render()` + HMR accept to all page entry files to make them standalone entries
- Fixed critical Intl polyfill crash: created `src/intl-shim.ts` to ensure `globalThis.Intl` exists before `@formatjs/intl-segmenter/polyfill-force.js` runs (PrimJS has no `Intl` global)
- Uncommented the `@formatjs/intl-segmenter/polyfill-force.js` import in `src/analysis.ts`
- DevTool screenshot verified: page renders title, width controls (+/− buttons), native `<text>` rendering, comparison cards
- Files changed: lynx.config.ts, src/intl-shim.ts (new), src/analysis.ts, pages/basic-height.tsx, pages/layout-with-lines.tsx, pages/shrinkwrap.tsx, pages/variable-flow.tsx, pages/accuracy.tsx, pages/demos/bubbles.tsx, pages/demos/dynamic-layout.tsx
- **Learnings for future iterations:**
  - PrimJS does not provide the `Intl` global — must create `globalThis.Intl = {}` before any polyfill that assumes it exists
  - `import './intl-shim'` before `import '@formatjs/intl-segmenter/polyfill-force.js'` works because ES module imports are evaluated in source order
  - Rspeedy multi-entry uses `source.entry` object syntax in lynx.config.ts — each key becomes `<key>.lynx.bundle`
  - Old dev server processes can hold port 3000; kill them with `pkill -f "rspeedy dev"` before restarting
  - DevTool screenshot only works on non-crashed sessions — check `get-console` for errors if screenshot times out
---

## 2026-03-29 - US-011 (VERIFIED)
- Fixed manually rendered lines: replaced `position: 'absolute'` on `<text>` with stacked `<view>` wrappers with fixed height
- Lynx `<text>` elements don't support `position: 'absolute'` — must wrap in `<view>` for positioning
- Removed invalid `numberOfLines` prop (not in Lynx TextProps)
- DevTool screenshot verified: page renders title, width controls, summary stats, manually rendered lines in bordered container, line details table
- tsc --build passes, pnpm build passes
- Files changed: pages/layout-with-lines.tsx (fixed line rendering), tasks/prd.json (mark passes: true)
- **Learnings for future iterations:**
  - In Lynx, `<text>` elements do not support `position: 'absolute'` — always wrap text in `<view>` for positioning
  - Stacked `<view>` with fixed `height: LINE_HEIGHT` is a reliable alternative to absolute positioning for line rendering
  - `numberOfLines` is not a valid prop on Lynx `<text>` — use Lynx-specific text truncation if needed
---

## 2026-03-29 - US-012 (VERIFIED)
- Simplified shrinkwrap demo to use native `<text>` rendering in bordered containers
- Fixed container height issue: removed `lineHeight` from `<text>` style (caused abnormal height), added `alignSelf: 'flex-start'`
- Used `shrinkwrapMaxWidth` (minimum maxWidth preserving lineCount) for container width instead of `shrinkwrapWidth` (widest line)
- DevTool screenshot verified: CSS fit-content (291px, orange) vs shrinkwrap (233px, green) clearly shows tighter container
- tsc --build passes, pnpm build passes
- Files changed: pages/shrinkwrap.tsx (simplified rendering), tasks/prd.json (mark passes: true)
- **Learnings for future iterations:**
  - Setting `lineHeight` on `<text>` in a `<view>` with `width` can cause unexpectedly tall containers in Lynx — omit if not needed
  - Always add `alignSelf: 'flex-start'` on fixed-width containers inside flex parents to prevent height stretching
  - For visual comparison demos, native `<text>` rendering is simpler and more honest than per-line rendering
---
